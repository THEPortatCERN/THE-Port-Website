// Set a CSS variable for the scrollbar width. This is used to calculate the layout.
document.documentElement.style.setProperty('--scrollbar-width', Math.max(window.innerWidth - document.documentElement.clientWidth + 1) + "px");
